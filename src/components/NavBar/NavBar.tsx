const NavBar = () => {
  return (
    <div>
      <button>About</button>
      <button>Members</button>
      <button>Gallery</button>
      <button>Signup</button>
    </div>
  );
};

export default NavBar;
